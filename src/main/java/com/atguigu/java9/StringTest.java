package com.atguigu.java9;

/**
 * String：JDK9之前：底层使用char[]；JDK9：底层使用byte[]
 * StringBuffer：JDK9之前：底层使用char[]；JDK9：底层使用byte[]
 * StringBuilder：JDK9之前：底层使用char[]；JDK9：底层使用byte[]
 * <p>
 * String：不可变的字符序列
 * StringBuffer：可变的字符序列；线程安全的，效率低
 * StringBuilder：可变的字符序列；线程不安全的，效率高
 */
public class StringTest {
}
